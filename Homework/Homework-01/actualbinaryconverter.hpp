#include <iostream>
#include <string>
#pragma once
// Functions for encoding the different inputs into binary strings//
std::string encodeInt(int number);
std::string encodeChar(char character);
std::string encodeFloat(float number);