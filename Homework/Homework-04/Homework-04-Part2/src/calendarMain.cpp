#include <iostream>
#include <iomanip>
#include <cmath>

int main()
{
    int month, year;
    std::cout << "Enter A Month (MM) and Year (YYYY):" << std::endl;
    std::cin >> month;
    std::cout << "Enter A Year (YYYY):" << std::endl;
    std::cin >> year;

       return 0;
}